{% extends 'layouts/base.twig' %}
{% block title %} {{title|capitalize|title}} {% endblock %}
{% block content %}
{{parent()}}
<div class="panel panel-primary">
	<div class="panel-heading">
		<h3 class="panel-title text-center">{{title|upper}}</h3>
	</div>
	<!-- Table -->
	<div class="table-responsive">
		<table class="table table-striped table-bordered table-hover">
			<thead>
				<tr>
					<th>id</th>
					<th>Nombre(s)</th>
					<th>N° de aulas</th>
					<th>Detalles</th>
				</tr>
			</thead>
			<tbody>
				{% for item in result %}
					<tr>
						<th>{{item['id']}}</th>
						<td>{{item.nombre|capitalize}}</td>
						<td>{{item.naula}}</td>
						<td class='text-left'><a href="edif_detail.php?edif={{item.id}}" target='_blank'><span class="fa fa-external-link"></span></a></td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
</div>
{% endblock %}
